name: Run CI on pgscatalog.calc

on:
  push:
    paths:
      - 'pgscatalog.calc/**.py'
  pull_request:
    paths:
      - 'pgscatalog.calc/**.py'

jobs:
  calc-ci:
    uses: ./.github/workflows/ci.yaml
    with:
      package-directory: "pgscatalog.calc"