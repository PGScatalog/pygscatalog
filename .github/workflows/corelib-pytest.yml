on:
  push:
    paths:
      - 'pgscatalog.corelib/**.py'
  pull_request:
    paths:
      - 'pgscatalog.corelib/**.py'

jobs:
  downloadapp-pytest:
    uses: ./.github/workflows/pytest.yaml
    with:
      package-directory: "pgscatalog.corelib"